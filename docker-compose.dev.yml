# Optional explicit dev selection; the default Compose file already supplies live editing.
services:
  app:
    build:
      args:
        NEXT_PUBLIC_ADMIN_MODE: live
    environment:
      MODE: dev
      DOCREVIEW_ADMIN_MODE: live
  web:
    environment:
      NEXT_PUBLIC_ADMIN_MODE: live

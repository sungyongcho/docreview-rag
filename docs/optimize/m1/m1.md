# Module 1

상세 최적화 기록은 [M1.2](m1-2.md), [M1.3](m1-3.md), [M1.4](m1-4.md)에 있다. `M1.1`(`parser.py`, `xref.py`)은 별도 최적화 패스를 진행하지 않아 체크포인트 기록이 없다.

[refactor]

- `R1-4`: `xref._in_tables`를 공개 `in_tables`로 승격했다 — [refactor.md](../refactor.md) 참고.
- `R1-6`: `chunk.compose_index_text`로 `index_text` 조합 규칙을 단일화하고, `_Unit`을 공개 `Unit`으로 바꿨으며, `edgar.CORPUS`가 `Settings.corpus_dir`를 읽게 했다.
- `R1-7`: `chunk.py`·`tables.py`·`edgar.py`·`parser.py`에 모듈 docstring을 추가했다.
- `R3-1`: `parse_seed_filings`와 `build_seed_batch_from_filings`를 추가해 M3의 두 청크 목표가 같은 파싱 스냅샷을 재사용하게 했다 — [refactor.md](../refactor.md) 참고.
- `R3-3`: 시드 레코드·배치·파싱/영속화 경계의 NumPyDoc을 최종 소스와 동기화했다.

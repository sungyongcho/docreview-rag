8/28
- 지금 이 프로젝트를 AI를 활용하기 좋도록 구조를 짜고 있는중이다.
- 포트폴리오용 배포/documentation에서, AI사용을 숨기지 않고, 필요한 부분에서는 적극 사용함을 알려야된다.
  - 물론 bm25, rrf, rerank, bi-encoder/cross-encoder 같은 개념은 내가 이해를 해야되기때문에 충분히 학습을 하는 시간을 가졌다.
- 철칙: AI 에게 모든것을 절대 맡기지 않는다. AI가 프로젝트에 접근 했을 때 프로젝트 구성을 잘 이해하고, 최대한 혼동이 없도록 함축적이고 구조가 잘 짜여진 프로젝트 베이스를 만드는것이 목표이다.
- 파이썬에서 가져다 쓸 수 있는데도 파서를 직접 구현 한 이유는 SEC에 대한 배경지식이 없었기 때문에 직접 구현을 선택하게 되었다.
- 내가 파서를 직접 구현하겠다고 결정 한 이유가 프로젝트 진행에 도움이 되었던건, 문서 자체 파싱로직을 보면서 SEC 내용 구조에 대해서 기반 지식이 없었기 때문도 있다 <- 여기서 '기반지식이 없다'는 태어나서 한번도 실제로 읽어본적이 없다는건데 이걸 포트폴리오에 넣기는 어떰.
- it was quite interesting to see that AI is actually typing out the orm codes while some orm commans gives out the options to generate the code for you


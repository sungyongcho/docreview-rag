# 버그픽스 2026-08-26 — M4로 넘어가기 전에 고쳐야 할 것들

## 이 문서는 무엇인가

M1부터 M3까지를 튜토리얼대로 완주하고 최적화까지 마친 뒤, "이제 M4로 넘어가도 되는가"를 판정하기 위해 코드베이스 전수 감사를 수행했습니다. 이때 튜토리얼 문서나 `new` 브랜치를 정답으로 삼지 않았습니다. 지금 `zero` 작업트리에 있는 코드 자체가 RAG 시스템으로서 정합한지만 보았습니다. 판정 근거는 전부 라이브 PostgreSQL(문서 20건, 청크 9,172건)에 대한 직접 측정입니다.

결론부터 말씀드리면 **M4 진입은 보류하는 것이 맞습니다.** 다만 그 이유가 흔히 예상하는 곳에 있지 않습니다. 검색 스택 자체는 놀라울 만큼 잘 만들어져 있습니다. RRF 융합, BM25 공식, 벡터 검색, 점수 집계 산술, 증거 무결성 — 전부 교과서 그대로입니다. 문제는 **검색을 재는 자(尺)가 망가져 있고, 코퍼스가 진짜 임베딩이 아닌 임시 대체물로 채워져 있다**는 점입니다.

M4는 검색 결과를 받아 답변을 생성하는 계층입니다. 자가 망가진 채로 M4를 올리면 이후에 무엇을 개선하든 개선됐는지 알 수 없게 됩니다. 그래서 지금 고칩니다.

이 문서는 지시서가 아니라 학습 문서입니다. 고쳐야 할 자리마다 "왜 이게 문제인가"를 먼저 세우고, 필요한 개념을 그 자리에서 설명한 뒤, 편집 방향을 드립니다. 완성된 코드를 붙여 넣지 않습니다. 직접 손으로 고치실 수 있도록 위치와 불변조건만 명확히 드립니다.

## 전체 지도

일곱 항목입니다. 앞의 네 개는 서로 독립이라 순서를 바꿔도 되고, 무엇보다 **재시드를 유발하지 않아** 지금 당장 고치고 바로 검증할 수 있습니다. 뒤의 세 개는 하나로 묶인 덩어리입니다.

```text
[1] coverage 채점 규칙          독립 · 지표 천장을 없앤다
[2] lexical 이중 스테밍          독립 · 인용 구문 검색을 되살린다
[3] retrieve() provider 재사용    독립 · M4가 밟을 지뢰를 미리 치운다
[4] 게이트 3종                   독립 · 안전망을 복구한다
      |
      v
[5] 청커 테이블 행 provenance     재시드 유발 · 인용 정밀도
[6] 재인덱싱 사이클              재시드 -> BM25 재빌드 -> sbert 재임베딩
[7] ablation 재실행              sbert · openai arm 포함
```

항목 5가 데이터베이스를 다시 채우게 만들기 때문에, 임베딩 교체(항목 6)와 반드시 한 사이클로 묶어야 합니다. 두 번 채우면 시간만 두 배로 듭니다.

## 읽기 전에 — 이 문서의 약속

모든 수치는 제가 실제로 돌려서 얻은 값입니다. 추정치나 "아마 이럴 것이다"는 없습니다. 각 항목의 **재현** 절에 있는 명령은 그대로 복사해 실행하시면 같은 숫자가 나옵니다. 만약 다른 숫자가 나온다면 그 사실 자체가 중요한 정보이니 멈추고 확인해 주세요.

반대로, 감사 과정에서 "이것도 버그 아니냐"고 제기됐지만 **검증 결과 버그가 아니었던 것들**이 꽤 됩니다. 이것들은 부록 A에 근거와 함께 적어 두었습니다. 나중에 같은 자리를 다시 파지 않으시도록 하기 위함입니다. 부록 B에는 반대로 **절대 건드리면 안 되는, 이미 잘 되어 있는 것들**을 적었습니다. 특히 항목 5 작업 중에 무심코 깨뜨리기 쉬운 것이 하나 있으니 꼭 확인해 주세요.

---

## 1. 채점 규칙 — 정답을 찾아왔는데 0점을 주는 자

### 증상

가장 먼저 이 표를 보시죠. 라이브 코퍼스에서 골든 24개 케이스를 채점한 결과입니다.

```text
positive cases: 24
  ceiling recall = 0.0  (완벽하게 검색해도 채점 불가): 7
    -> m3c-04, m3c-05, m3c-07, m3c-12, m3c-14, m3c-19, m3c-28
  ceiling recall < 1.0  (부분적으로만 채점 가능)     : 1  -> m3c-24
  ceiling recall = 1.0                              : 16

  => 이 코퍼스와 이 지표로 달성 가능한 최대 recall ~= 0.6875
```

"ceiling"은 천장이라는 뜻입니다. 검색기가 신이 되어 정답을 항상 1등으로 가져와도 그 케이스에서 받을 수 있는 최대 점수를 말합니다. **24개 중 7개는 그 천장이 0점입니다.** 무슨 짓을 해도 점수를 받을 수 없다는 뜻입니다.

그래서 `recall@5 = 0.5208`이라는 우리의 BM25 성적표는 100점 만점에 52점이 아닙니다. **68.75점 만점에 52점**, 즉 달성 가능한 것의 76%입니다. 성적표의 분모 자체가 틀려 있었던 것입니다.

### 재현

말보다 실제 사례가 빠릅니다. `m3c-28`은 "NVIDIA의 2024 회계연도 매출과 순이익은 얼마인가"를 묻습니다. 커밋된 평가 아티팩트를 열어보시죠.

```bash
python3 -c "import json; d=json.load(open('data/eval_runs/20260824T203336Z-structure-1200-lexical-bm25.json')); c=[x for x in d['cases'] if x['golden']['id']=='m3c-28'][0]; print('score:', c['score']); print('rank5 body head:', c['hits'][4]['body'][:200])"
```

기록된 점수는 recall 0.0, hit 0.0, MRR 0.0입니다. 그런데 5위로 가져온 청크의 본문에는 `| Revenue | $ | 60,922 |`와 `| Net income | $ | 29,760 |`가 **글자 그대로** 들어 있습니다. 질문이 요구한 바로 그 숫자 두 개입니다.

검색기는 정답을 찾아왔습니다. 채점기가 못 알아본 것입니다.

### 원인

원인은 `app/evals/scoring.py:44-60`의 `span_iou` 한 함수에 있습니다. 이 함수가 "이 청크가 정답인가"를 판정합니다.

```python
overlap = max(0, min(golden.end_char, hit.end_char) - max(golden.start_char, hit.start_char))
if overlap == 0:
    return 0.0
union = (golden.end_char - golden.start_char) + (hit.end_char - hit.start_char) - overlap
return overlap / union
```

여기서 결정적인 것은 **분모**입니다.

> **개념 — IoU(Intersection over Union)와 그 분모가 하는 일**
>
> IoU는 두 영역이 얼마나 겹치는지를 재는 고전적인 척도입니다. 이름 그대로 교집합을 합집합으로 나눕니다. 원래는 컴퓨터 비전에서 왔습니다. 사진 속 고양이를 네모로 표시하는 모델을 평가할 때, 모델이 그린 네모와 사람이 그린 정답 네모가 얼마나 일치하는지를 IoU로 잽니다.
>
> 이때 IoU가 좋은 척도인 이유는 **양쪽 다 벌점을 주기 때문**입니다. 네모를 너무 작게 그려도(교집합이 작아짐), 너무 크게 그려도(합집합이 커짐) 점수가 떨어집니다. 고양이 한 마리를 잡겠다고 사진 전체를 네모로 칠하는 꼼수를 IoU가 막아줍니다. 대칭적인 척도라서 그렇습니다.
>
> 그런데 우리가 재려는 것은 그게 아닙니다. 우리는 "이 청크 안에 정답이 들어 있는가"를 묻고 싶습니다. 청크가 정답보다 얼마나 큰지는 **검색기의 잘못이 아니라 청킹 전략의 결과**입니다. 검색기를 평가하는 자리에서 청킹의 특성에 벌점을 주고 있으면, 그 성적표는 두 가지를 뒤섞어 재는 셈입니다.
>
> 그래서 우리가 바꿀 방향은 분모를 **합집합에서 정답 span 길이로** 옮기는 것입니다. 이렇게 하면 척도는 비대칭이 되고, 의미가 "정답의 몇 퍼센트가 이 청크 안에 들어 있는가"로 바뀝니다. 정보 검색 문헌에서는 이런 형태를 coverage 또는 recall-oriented overlap이라고 부릅니다.

그러면 우리 데이터에서 분모가 왜 그렇게 커지는지 봐야 합니다. 청크의 `start_char`와 `end_char`는 정제된 텍스트가 아니라 **원본 HTML 문자열**의 좌표입니다.

```text
kind    count   span p50   span p90   body p50   span/body p50
text    8,083      1,403      4,212        781            1.90
table   1,089     13,617     46,631        401           31.93
```

텍스트 청크는 본문 781자에 원문 1,403자를 인용합니다. 배율 1.9배 정도면 태그가 좀 섞였구나 싶은 수준입니다. 그런데 **테이블 청크는 본문 401자에 원문 13,617자를 인용합니다. 34배입니다.** `<td style="...">` 같은 마크업이 실제 숫자보다 압도적으로 많기 때문입니다.

`m3c-07`이 가장 극단적입니다. 413자짜리 정답이 들어 있는 청크의 원문 span이 약 46,000자입니다. IoU를 계산하면 `413 / 46000 ≈ 0.0089`가 나옵니다. 임계값 0.05의 6분의 1에도 못 미칩니다.

### 결정적인 사실

여기서 가장 중요한 발견을 말씀드립니다. 죽은 정답 span 13개 전부에 대해, 그것을 **완전히 품고 있는 청크가 코퍼스에 정확히 하나씩 존재합니다.**

```text
  m3c-04 gold0: goldlen=757  overlapping_chunks=1 bestIoU=0.0271 bestCov=1.000
  m3c-05 gold1: goldlen=240  overlapping_chunks=1 bestIoU=0.0152 bestCov=1.000
  m3c-07 gold0: goldlen=413  overlapping_chunks=1 bestIoU=0.0089 bestCov=1.000
  m3c-12 gold1: goldlen=2280 overlapping_chunks=1 bestIoU=0.0441 bestCov=1.000
  m3c-28 gold0: goldlen=639  overlapping_chunks=1 bestIoU=0.0140 bestCov=1.000
  ... (13개 전부 bestCov = 1.000)
```

`bestCov = 1.000`이 뜻하는 바는 명확합니다. **정답의 100%가 그 청크 안에 들어 있습니다.** 겹치는 청크가 아예 없는 정답은 단 하나도 없습니다.

이 사실이 왜 중요한가 하면, **골든 데이터셋에는 아무 문제가 없다**는 것을 증명하기 때문입니다. 정답 좌표도 정확하고, 청킹도 정답을 쪼개지 않았습니다. 손실은 전부 채점 규칙 하나에서 발생했습니다. 규칙만 바꾸면 데이터를 한 글자도 건드리지 않고 7개 케이스가 되살아납니다.

### 불변조건

고친 뒤 반드시 성립해야 하는 성질은 하나입니다.

**정답 span을 완전히 품은 청크는, 그 청크가 아무리 크더라도 relevant로 판정되어야 합니다.**

여기에 하나를 덧붙입니다. 서로 다른 스냅샷(`source_sha256`이 다른 경우)이나 다른 문서의 청크는 겹치는 좌표를 가지더라도 절대 relevant가 되면 안 됩니다. 이 가드는 `span_iou`의 첫 두 줄에 이미 있으니 **그대로 보존**하셔야 합니다.

### 수정

손댈 곳은 `app/evals/scoring.py` 안의 세 자리입니다.

**첫째, 임계값(`:10`).** `IOU_THRESHOLD: Final[float] = 0.05`를 coverage 척도에 맞는 이름과 값으로 바꿉니다. 이 상수는 `app/` 전체에서 `:105` 한 곳에서만 참조되므로 이름을 바꿔도 파급이 없습니다.

값은 `0.5`를 권합니다. 다만 여기서 정직하게 말씀드릴 것이 있습니다. **이 코퍼스에서는 `cov >= 0.5`와 `cov > 0`이 완전히 같은 숫자를 냅니다.** 청크 span이 정답 span보다 워낙 커서, 조금이라도 겹치면 무조건 절반 이상을 덮어버리기 때문입니다. 즉 0.5라는 값은 데이터로 검증된 값이 **아닙니다.** 나중에 청킹이 더 촘촘해졌을 때를 대비한 안전 여유입니다. 주석에도 그렇게 적어 주세요. 튜닝된 값인 것처럼 쓰면 나중에 그 근거를 찾다가 시간을 버리게 됩니다.

**둘째, 척도 함수(`:44-60`).** 분모를 합집합에서 정답 span 길이로 바꿉니다. 함수 이름도 `span_iou`가 아닌 것으로 바꾸는 편이 정직합니다. 이름이 IoU인데 IoU가 아닌 것을 계산하면 다음에 읽는 사람이 반드시 헷갈립니다. 이름을 바꾸시면 `tests/evals/test_02_scoring.py:74-75`가 옛 이름을 참조하고 있으니 함께 손봐야 합니다.

**셋째, 판정 술어(`:103-105`).** 현재 `score_case` 안에 중첩된 클로저입니다.

```python
def _is_relavelent(golden: GoldenSpan, hit: ChunkHit) -> bool:
    """Return whether one hit clears the IoU relevance threshold."""
    return span_iou(golden, hit) >= IOU_THRESHOLD
```

이름에 오타가 있습니다. `_is_relavelent`가 아니라 `_is_relevant`입니다. 고치는 김에 함께 바로잡으시죠. docstring도 새 규칙을 설명하도록 갱신해야 합니다.

### 반드시 함께 해야 하는 일 — 과거 결과와의 분리

이것을 빼먹으면 조용히 잘못된 결론에 도달하게 됩니다. 왜 그런지 설명드립니다.

> **개념 — 왜 평가 설정(config)이 비교의 열쇠인가**
>
> 회귀 판정이란 "이번 실행이 지난번보다 나빠졌는가"를 자동으로 알려주는 장치입니다. 그러려면 "지난번"이 무엇인지 골라야 합니다. `app/evals/regression.py:352-361`의 `latest_comparable_baseline`이 그 일을 합니다.
>
> 이 함수가 기준선을 고르는 조건은 딱 둘입니다. `suite`가 같고, `config`가 **바이트 단위로 정확히 같을 것**. config는 정규화된 JSON으로 저장되므로 키 순서나 공백 차이로 갈라지지 않습니다. 반대로 말하면, **config에 기록되지 않은 것은 비교 대상을 가르지 못합니다.**
>
> 지금 config에는 청킹 전략, 목표 글자 수, 검색 전략, 임베딩 provider, `k`, `rrf_k` 같은 것들이 들어 있습니다. 그런데 **채점 규칙은 어디에도 없습니다.** 그러니 규칙을 IoU에서 coverage로 바꾸고 같은 arm을 다시 돌리면, 새 결과가 옛 IoU 기준선과 정확히 매칭됩니다. 그리고 recall이 0.5208에서 0.5417로 올라간 것을 **"개선됐다"고 보고합니다.** 실제로는 자를 바꿨을 뿐인데 말입니다.

그래서 `app/evals/ablation.py:192-210`의 `ExperimentConfig.to_dict()`에 `"scoring"` 블록을 추가해야 합니다. 채점 규칙 이름과 임계값을 담으면 됩니다. config가 달라지므로 새 실행과 옛 실행이 강제로 분리됩니다.

여기서 흔한 오해를 하나 미리 짚겠습니다. "`RAW_ARTIFACT_SCHEMA_VERSION`(`retrieval_eval.py:69`)을 올리면 되지 않나?" 하실 수 있는데, **그것으로는 안 됩니다.** `schema_version`은 아티팩트 최상위에 있고 `config` 안에 있지 않습니다. 기준선 선택 쿼리는 `config`만 봅니다. 버전을 올리는 것은 아티팩트가 바뀌었다는 기록으로서는 의미가 있지만, 기준선을 가르지는 못합니다.

한 가지 다행인 점은 `run_ablation`(`ablation.py:454-455`)이 `evaluation.config != config.to_dict()`이면 에러를 내도록 되어 있다는 것입니다. `to_dict()`에 필드를 추가하면 아티팩트와 config가 자동으로 함께 움직입니다. 한쪽만 고쳐서 어긋날 걱정은 하지 않으셔도 됩니다.

### 기대 효과

바꾸고 나면 이렇게 됩니다.

| | 현재 IoU >= 0.05 | coverage >= 0.5 |
|---|---|---|
| 천장이 0인 케이스 | **7 / 24** | **0 / 24** |
| 달성 가능한 최대 recall | 0.6875 | **1.0000** |
| bm25 recall@5 | 0.5208 | 0.5417 |
| bm25 hit@5 | 0.5417 | 0.5417 (불변) |
| bm25 MRR | 0.3951 | 0.3951 (불변) |
| ts_rank_cd recall@5 | 0.2708 | 0.3333 |
| ts_rank_cd hit@5 | 0.2917 | 0.3333 |
| ts_rank_cd MRR | 0.1715 | 0.2132 |

여기서 숫자를 해석해 드리겠습니다.

**BM25의 겉보기 성적은 거의 안 움직입니다.** hit@5와 MRR은 아예 그대로이고 recall만 0.0208 오릅니다. 0.0208은 우연이 아니라 `1/48`, 즉 정답 span 하나가 살아난 값입니다. 왜 이것밖에 안 오르냐면, BM25가 원래 못 찾던 케이스는 규칙을 바꿔도 여전히 못 찾기 때문입니다. 규칙 변경은 "찾았는데 인정 못 받던" 경우만 구제합니다.

**진짜 성과는 마지막 두 줄이 아니라 첫 두 줄입니다.** 천장이 0.6875에서 1.0으로 올라갔습니다. 앞으로 검색기를 개선했을 때 그 개선이 숫자에 반영될 수 있는 공간이 31%포인트 넓어진 것입니다. M4에서 재순위화나 쿼리 확장을 붙였을 때 효과를 볼 수 있느냐가 여기 달려 있습니다.

**ts_rank_cd가 상대적으로 많이 오르는 것**도 의미가 있습니다. 이 경로는 원래 큰 테이블 청크를 자주 물어오는 경향이 있었는데, 그것들이 전부 0점 처리되고 있었습니다.

### 파급 — 무엇을 함께 고쳐야 하나

먼저 안심하셔도 되는 것부터 말씀드립니다. **지표 배관은 전혀 손댈 필요가 없습니다.** `recall_at_k`, `hit_rate_at_k`, `mrr`, `score_suite` 전부 `CaseScore`의 float만 받아 평균 냅니다. 판정 규칙이 바뀌어도 시그니처와 반환 타입이 그대로라 `retrieval_eval.py`, `ablation.py`, `regression.py` 모두 무사합니다.

> **개념 — 케이스별 점수를 평균 내는 두 가지 방식**
>
> 이 코드는 케이스마다 recall을 구한 뒤 케이스 개수로 나눕니다. 이것을 macro-average라고 부릅니다. 대안은 모든 정답 span을 한 통에 모아 "맞힌 span 수 / 전체 span 수"를 구하는 micro-average입니다.
>
> 둘의 차이는 **정답이 여러 개인 케이스의 무게**입니다. macro에서는 정답 span이 1개인 케이스든 2개인 케이스든 똑같이 1/24만큼 기여합니다. micro에서는 span이 많은 케이스가 더 크게 기여합니다. 어느 쪽이 옳다기보다, 케이스를 하나의 질문으로 보느냐 증거 단위로 보느냐의 차이입니다.
>
> 우리 코드는 macro를 씁니다. 여기서 실용적인 함의가 하나 나옵니다. 24개 케이스를 macro-average하고 케이스당 정답이 1~2개이므로, **이 지표가 표현할 수 있는 가장 작은 눈금은 `0.5 / 24 = 0.0208`입니다.** 소수점 여섯 자리로 출력되고 있지만 실제 분해능은 소수점 둘째 자리입니다. 이 사실은 항목 7에서 다시 중요해집니다.

다시 써야 하는 테스트는 `tests/evals/test_02_scoring.py`에서 **정확히 두 개**입니다.

`test_exact_half_open_span_has_unit_iou`(`:47-50`)는 `IOU_THRESHOLD == 0.05`와 완전 일치 시 1.0을 단언합니다. 완전 일치는 coverage에서도 1.0이므로 실질적으로는 상수 이름과 값만 갱신하면 됩니다.

`test_partial_overlap_uses_union_and_includes_the_threshold`(`:53-61`)가 진짜 작업입니다. 이 테스트는 정답 `[100, 200)`에 대해 `[190, 300)`이 IoU 정확히 0.05가 되도록 좌표를 짜 놓고, 경계값이 포함(`>=`)임을 확인합니다. coverage로 바꾸면 이 히트의 커버리지는 `10/100 = 0.1`이 되어 0.5 임계값 아래로 떨어집니다. **경계 서사 전체를 coverage 눈금에 맞는 새 좌표로 다시 설계**하셔야 합니다. 테스트 이름의 `uses_union`도 더는 사실이 아니니 함께 바꾸세요.

그 외에는 대부분 그대로 통과합니다. 특히 `:111-119`의 "넓은 청크 하나가 정답 두 개를 덮는" 케이스는 IoU에서 0.25였는데 coverage에서 1.0이 되어 **오히려 의도가 더 선명해집니다.**

주의 깊게 보실 것이 하나 있습니다. `test_rechunked_and_reidentified_hits_preserve_scores`(`:164-177`)는 **아슬아슬하게 통과합니다.** `config_b`의 두 번째 히트가 커버리지 0.4로 임계값 아래로 떨어지지만, 1위 히트가 0.6을 덮어서 최종 단언(`recall == 1.0`, `mrr == 1.0`)은 유지됩니다. 임계값을 0.5보다 올리시면 이 테스트가 깨집니다. 기억해 두세요.

마지막으로 하나 더. `tests/agent/test_06_decompose.py:131-141`이 `CaseScore`를 손으로 생성합니다. **기본값 없는 필드를 `CaseScore`에 추가하면 이 자리가 깨집니다.** 지금은 pyright에서 제외된 디렉터리라 눈에 안 띌 수 있으니 미리 알고 계세요.

### 문서 게이트의 함정 — 이건 꼭 아셔야 합니다

`scripts/check_doc_code.py`가 문서 속 코드 블록을 소스와 대조합니다. 그런데 **작업트리와 대조하는 게 아닙니다.**

```toml
reference_revision = "a5fa6efa753be1d08f86c352caf6c45601568807"
```

`docs/project/learning.toml`에 박혀 있는 이 커밋을 `git show {revision}:{path}`로 꺼내서 비교합니다(`sync_tutorial_code.py:132-142`). 결과는 두 가지입니다.

첫째, **`scoring.py`를 고쳐도 게이트는 초록으로 남습니다.** 안심할 일이 아니라 조심할 일입니다. 문서의 코드 블록을 갱신하려면 새 `scoring.py`가 담긴 커밋으로 핀을 올려야 하고, 그 전까지는 문서와 코드가 어긋나 있어도 아무도 알려주지 않습니다.

둘째, **핀은 이미 어긋나 있습니다.** 핀 시점의 `scoring.py`에는 모듈 수준에 `def _is_relevant`가 있는데, 지금 작업트리에는 `score_case` 안에 중첩된 `_is_relavelent`가 있습니다. 철자까지 다릅니다. 게이트는 이 차이를 볼 수 없습니다.

문서 쪽에서 갱신 대상은 `docs/{en,ko}/m3-evals/02-spec.md:103-113`입니다. 여기에 IoU 공식이 **규범 계약**으로 명시되어 있습니다. 그 밖에 `tutorial/03-scoring.md`, `03-build.md`, `01-findings.md`, `docs/project/planning/*`, `eval-report.md`, `failure-analysis.md` 등 약 15개 파일이 IoU를 언급합니다. 그리고 `05-verify.md:19`와 `00-README.md:44`가 이 테스트 파일의 `55 passed`를 못 박고 있으니, 케이스 수가 바뀌면 이 숫자도 함께 고쳐야 합니다.

### 확인

```bash
uv run pytest -o addopts="" tests/evals/test_02_scoring.py -q
```

그리고 천장이 실제로 사라졌는지 직접 재보세요. 항목 7의 재검증 절에 스크립트 형태로 정리해 두었습니다. **7이 0이 되고 최대 달성 recall이 1.0이 나와야 합니다.**

### 정리

채점 규칙의 분모를 합집합에서 정답 길이로 바꿨습니다. 이것으로 "정답을 찾아왔는데 청크가 커서 0점"이던 7개 케이스가 살아나고, 지표의 천장이 0.6875에서 1.0으로 올라갑니다. 함께 `ExperimentConfig.to_dict()`에 채점 규칙을 기록해서 옛 결과와 새 결과가 섞이지 않게 막았습니다. 성적표 숫자 자체는 크게 안 움직이지만, **앞으로의 개선이 숫자에 반영될 수 있는 공간을 확보한 것**이 진짜 성과입니다.

---

## 2. 인용 구문 검색이 항상 0건인 이유

### 증상

큰따옴표로 묶은 구문 검색이 **단 한 건도 반환하지 않습니다.** 오류도 나지 않고, 그냥 조용히 빈 결과가 나옵니다.

### 재현

PostgreSQL에 직접 물어보면 무슨 일이 벌어지는지 한눈에 보입니다.

```bash
psql "postgresql://filing:filing@localhost:5432/filing" -c "select websearch_to_tsquery('english','share repurchase')::text as parsed, to_tsquery('english', replace(websearch_to_tsquery('english','share repurchase')::text,'&','|'))::text as current, to_tsquery('simple', replace(websearch_to_tsquery('english','share repurchase')::text,'&','|'))::text as fixed"
```

결과입니다.

```text
websearch_to_tsquery('english','share repurchase')  ->  'share' & 'repurchas'
현재 코드가 만드는 것                                ->  'share' | 'repurcha'
'simple'로 재파싱하면                                ->  'share' | 'repurchas'
```

`'repurchas'`가 `'repurcha'`로 바뀐 것이 보이시나요. **`s`가 하나 더 떨어져 나갔습니다.** 그리고 인덱스에는 `'repurchas'`로 저장되어 있으니, `'repurcha'`는 아무것도 매치하지 않습니다.

실제 매치 건수로 확인하면 이렇습니다.

```text
인용 구문 "share repurchase":  현재    0건  /  simple    68건
일반 검색  share repurchase:   현재  884건  /  simple  1027건
```

일반 검색에서 차이 나는 143건은 **`repurchase` 쪽으로만 매치되던 청크들**입니다. 지금은 `share`만 살아 있어서 `share`가 없는 문서는 통째로 놓치고 있었습니다. 인용 구문 쪽은 두 lexeme이 `<->`로 붙어 있어 한쪽이 죽으면 전체가 죽습니다. 그래서 0건입니다.

`expenses` 같은 흔한 단어도 당합니다.

```text
'expenses' -> 'expens' -> 'expen'
```

### 개념 — 왜 이런 일이 생기는가

이 버그를 이해하려면 PostgreSQL 전문 검색의 파이프라인을 알아야 합니다.

> **개념 — tsvector, tsquery, 그리고 lexeme**
>
> PostgreSQL은 문서를 그대로 검색하지 않습니다. 먼저 문서를 **lexeme(어휘소)의 집합**으로 바꿔 저장합니다. 이 자료형이 `tsvector`입니다. 우리 스키마에서는 `chunks.content_tsv`가 그것이고, `app/db/models.py:84-88`에서 `to_tsvector('english', index_text)`로 자동 생성되는 컬럼입니다.
>
> lexeme은 단어의 어간입니다. `expenses`, `expense`, `expensed`가 전부 `expens`라는 하나의 lexeme으로 접혀 들어갑니다. 이 접는 작업을 **스테밍(stemming)**이라 하고, PostgreSQL은 Snowball이라는 알고리즘을 씁니다. 덕분에 사용자가 `expense`로 검색해도 `expenses`가 든 문서를 찾을 수 있습니다.
>
> 질의 쪽도 마찬가지로 lexeme으로 바뀝니다. 이 자료형이 `tsquery`입니다. 그리고 `content_tsv @@ tsquery` 연산자가 둘을 맞춰봅니다. **양쪽이 같은 방식으로 접혀 있어야 만날 수 있습니다.**
>
> 여기서 결정적인 것이 `'english'`와 `'simple'`의 차이입니다. `'english'`는 스테밍을 하는 설정이고, `'simple'`은 **소문자로만 바꾸고 스테밍은 하지 않는** 설정입니다.

> **개념 — 세 가지 질의 파서의 역할 분담**
>
> PostgreSQL은 사용자 입력을 `tsquery`로 바꾸는 함수를 여러 개 제공합니다. 셋만 알면 됩니다.
>
> `plainto_tsquery`는 입력을 단어로 쪼개 전부 `&`(AND)로 잇습니다. 단순하지만 사용자가 따옴표나 마이너스를 쓸 수 없습니다.
>
> `websearch_to_tsquery`는 검색 엔진 문법을 이해합니다. 큰따옴표는 구문 검색(`<->`)으로, `-단어`는 배제(`!`)로, `or`는 `|`로 번역해 줍니다. **우리 코드가 쓰는 것이 이것**이고, 사용자 입력을 안전하게 받는 올바른 선택입니다.
>
> `to_tsquery`는 가장 낮은 수준입니다. 이미 연산자가 들어 있는 **tsquery 문법 문자열**을 받아 파싱합니다. 즉 앞의 둘과 달리 자연어를 받는 함수가 아닙니다.
>
> 그리고 여기가 핵심입니다. `to_tsquery`는 문자열을 파싱하면서 **그 안의 단어들에 다시 스테밍을 적용합니다.** 이미 스테밍된 결과를 넣으면 두 번 접히는 것입니다.

### 원인

`app/retrieval/lexical.py:171-172`가 이렇게 되어 있습니다.

```python
parsed = func.websearch_to_tsquery(TEXT_SEARCH_CONFIG, query)
tsquery = func.to_tsquery(TEXT_SEARCH_CONFIG, func.replace(cast(parsed, Text), "&", "|"))
```

의도는 이렇습니다. `websearch_to_tsquery`가 모든 단어를 `&`로 이어버리면 다섯 단어짜리 질문은 다섯 어간을 모두 가진 청크만 찾게 되는데, 실제 코퍼스에서 그런 청크는 없었습니다. 커밋 `caddc70`의 메시지에 그 측정이 남아 있습니다. 모든 평가에서 `recall@5 = 0.000`이었고, 빈 결과는 오류가 아니라서 아무도 알아채지 못했습니다. 그래서 `&`를 `|`로 바꿔 완화한 것입니다. **이 판단 자체는 옳고, 이 문서에서 되돌리지 않습니다.**

문제는 재파싱에 쓴 설정입니다. `TEXT_SEARCH_CONFIG`가 `"english"`라서, 이미 `'repurchas'`로 접힌 것을 `to_tsquery('english', ...)`가 한 번 더 접어 `'repurcha'`로 만들어 버립니다.

여기서 마음에 걸리실 만한 지점을 미리 해소하겠습니다. **"영어 설정을 빼면 스테밍이 안 되는 것 아닌가?"** 걱정하실 수 있는데, 그렇지 않습니다. 스테밍은 이미 앞 단계 `websearch_to_tsquery('english', ...)`에서 끝났습니다. 재파싱 단계에서 필요한 것은 **연산자 구조를 읽어들이는 것뿐**이지 단어를 다시 처리하는 것이 아닙니다. `'simple'`은 정확히 그 일만 합니다.

### 불변조건

**첫 파싱이 만든 lexeme은 재파싱을 거쳐도 글자 그대로 보존되어야 합니다.** 바뀌어도 되는 것은 연산자(`&` → `|`)뿐입니다.

### 수정

`app/retrieval/lexical.py:172`에서 `to_tsquery`에 넘기는 설정만 `'simple'`로 바꾸면 됩니다. 첫 줄의 `websearch_to_tsquery`는 **반드시 `'english'`를 유지**해야 합니다. 스테밍은 거기서 일어나야 하니까요.

`TEXT_SEARCH_CONFIG` 상수 하나를 두 곳에서 쓰고 있으니, 상수를 둘로 나누는 편이 의도가 드러납니다. 이름으로 각각의 역할이 보이게 지으시면 좋겠습니다. 그리고 왜 재파싱만 `'simple'`인지 반드시 주석으로 남기세요. 이유를 모르면 다음 사람이 "일관성 없다"며 되돌려 놓습니다.

### 문서도 함께 고쳐야 합니다

이 항목에는 특별한 점이 있습니다. **출하된 문서가 사실이 아닌 것을 보장하고 있습니다.**

`docs/en/m2-retrieval/tutorial/03-retrieval-paths.md:508`에 이렇게 적혀 있습니다.

> Quoted phrases survive the relaxation because `websearch_to_tsquery` compiles them to the `<->` operator, and only `&` is rewritten.

앞부분은 맞습니다. 구문은 `<->`로 컴파일되고 `&` 치환은 그것을 건드리지 않습니다. **하지만 구문이 살아남지는 못합니다.** 치환 다음에 오는 재파싱이 lexeme을 뭉개기 때문입니다. 저자가 예상하지 못한 경로로 보장이 깨진 것입니다.

같은 문장이 `03-build.md:896`과 한국어판 `03-retrieval-paths.md:474`에도 있습니다. 세 곳 다 고쳐야 합니다.

### 부정 검색은 고치지 않습니다

감사 중에 `-term` 배제도 깨져 있다는 지적이 나왔습니다. `revenue -risk`가 `'revenu' & !'risk'`에서 `'revenu' | !'risk'`로 바뀌어, 의도한 985건 대신 7,278건을 매치합니다.

숫자는 사실입니다. **그런데 이것은 버그가 아니라 기록된 결정입니다.** 커밋 `caddc70`이 명시적으로 이렇게 적었습니다.

> One semantic is knowingly weakened: an explicit `-term` exclusion is relaxed along with everything else.

같은 문장이 출하 문서 세 곳에 실려 있습니다. 알고 받아들인 트레이드오프이므로 이 문서에서 되돌리지 않습니다. 덧붙이자면 골든 28개 질문 중 `-`를 쓰는 것은 **하나도 없어서** 측정에 미치는 영향도 0입니다.

### 확인

```bash
uv run pytest -o addopts="" tests/retrieval/test_04_lexical.py -q
```

그리고 인용 구문이 실제로 살아났는지 직접 확인하세요.

```bash
uv run python -m app.retrieval --query '"share repurchase"' --k 5 --provider deterministic
```

### 정리

재파싱 설정 한 단어를 `'english'`에서 `'simple'`로 바꿔서 이중 스테밍을 없앴습니다. 인용 구문 검색이 0건에서 68건으로 되살아나고, 일반 검색에서도 143건을 더 찾습니다. 골든 성적으로는 recall은 그대로지만 **MRR이 0.1715에서 0.2049로 19.5% 올랐습니다.** 순위가 좋아졌다는 뜻입니다. 그리고 사실이 아니었던 문서의 보장 문구도 함께 바로잡았습니다.

---

## 3. `retrieve()`가 부를 때마다 임베딩 provider를 새로 만든다

### 증상

지금은 아무 증상이 없습니다. 이것이 이 항목의 성격입니다. **M4를 시작하는 순간 발현할 지뢰**를 미리 치우는 작업입니다.

### 재현

```bash
uv run python -c "
from app.retrieval.embeddings import get_embedding_provider
from app.config import Settings
s = Settings(_env_file=None, embedding_provider='openai', openai_api_key='sk-test-not-real')
ps = [get_embedding_provider(s) for _ in range(3)]
print('distinct providers   :', len({id(p) for p in ps}))
print('distinct http clients:', len({id(p._client) for p in ps}))
print('timeout / retries    :', ps[0]._client.timeout, '/', ps[0]._client.max_retries)
"
```

```text
distinct providers   : 3
distinct http clients: 3
timeout / retries    : Timeout(connect=5.0, read=600, write=600, pool=600) / 2
```

세 번 부르면 provider도 셋, HTTP 클라이언트도 셋입니다. 그리고 아무도 이것들을 닫지 않습니다.

### 원인

`app/retrieval/service.py:164` 한 줄입니다.

```python
active_provider = provider or get_embedding_provider()
```

`get_embedding_provider`는 캐시가 없는 평범한 팩토리 함수입니다. `@lru_cache`는 `get_settings`에만 붙어 있습니다(`app/config.py:38`). 그러니 `provider`를 안 넘기면 호출할 때마다 새로 만듭니다.

> **개념 — HTTP 커넥션 풀이 왜 인스턴스마다 따로 생기는가**
>
> `AsyncOpenAI`는 내부에 `httpx.AsyncClient`를 하나 들고 있습니다. 이 클라이언트가 TCP 커넥션 풀을 관리합니다. HTTPS 요청 하나를 보내려면 TCP 3-way handshake와 TLS 핸드셰이크를 거쳐야 하는데, 이게 왕복 시간의 상당 부분을 먹습니다. 풀은 한 번 맺은 연결을 재사용해서 그 비용을 없앱니다.
>
> 그런데 클라이언트를 매번 새로 만들면 풀도 매번 새로 생깁니다. 재사용할 연결이 없으니 매 쿼리가 핸드셰이크부터 시작합니다. 게다가 옛 클라이언트를 아무도 닫지 않으므로 소켓이 GC와 프로세스 종료에 맡겨집니다.
>
> sbert에서는 훨씬 나쁩니다. `SentenceTransformerEmbeddingProvider`는 인스턴스마다 인코더를 지연 로딩하는데, 인스턴스가 매번 새로 생기면 **쿼리마다 MiniLM 모델을 디스크에서 다시 읽어 올립니다.** 밀리초 단위 작업이 초 단위가 됩니다.

여기에 타임아웃 문제가 겹칩니다. `AsyncOpenAI`를 타임아웃 지정 없이 만들면 SDK 기본값인 read 600초, 재시도 2회가 적용됩니다. 최악의 경우 요청 하나가 30분 가까이 코루틴과 그것이 공유하는 DB 세션을 붙들고 있을 수 있습니다.

### 왜 지금은 안 터지는가

현재 `retrieve()`를 부르는 곳은 두 군데뿐이고, 둘 다 provider를 명시적으로 넘깁니다.

```text
app/retrieval/__main__.py:152    provider=provider,
app/evals/retrieval_eval.py:805  provider=provider,
```

`provider=None` 기본 경로를 타는 코드가 지금은 없습니다. **그런데 M4의 서비스 계층은 정확히 `retrieve(session, query)` 형태로 부르게 됩니다.** 그게 이 함수의 기본 사용법이고, docstring도 "생략하면 설정된 provider를 쓴다"고 안내하고 있으니까요.

### 불변조건

**한 프로세스 안에서 같은 설정이라면 provider 인스턴스는 재사용되어야 합니다.**

### 수정

두 방향이 있습니다. 어느 쪽을 고르실지는 취향보다 설계 판단입니다.

> **개념 — 캐싱과 의존성 주입 중 무엇이 이 자리에 맞는가**
>
> `get_embedding_provider`에 `@lru_cache`를 붙이는 것이 가장 적은 변경입니다. `get_settings`가 이미 그렇게 하고 있으니 일관성도 있습니다. 다만 함정이 하나 있습니다. `lru_cache`는 인자를 키로 쓰는데, `Settings`가 해시 가능해야 하고, 테스트에서 환경변수를 바꿔가며 호출할 때 **옛 인스턴스가 계속 돌아오는** 문제가 생길 수 있습니다. 그래서 캐시를 비우는 수단을 함께 마련해야 합니다.
>
> 다른 방향은 `retrieve()`에서 `provider` 기본값을 아예 없애고 **필수 인자로 만드는 것**입니다. 그러면 provider의 수명을 호출자가 책임지게 됩니다. 지금 두 호출부는 이미 넘기고 있으니 당장 깨지는 곳도 없습니다. M4에서는 서비스 시작 시 한 번 만들어 앱 수명 동안 들고 있으면 됩니다. 이쪽이 명시적이고, 나중에 provider를 교체하거나 테스트에서 가짜를 넣기도 쉽습니다.
>
> 규모가 커질수록 후자가 유리합니다. 다만 학습 프로젝트에서 편의를 유지하고 싶으시면 전자로 가되 캐시 무효화를 반드시 함께 만드세요.

타임아웃은 별개로 손보시면 좋습니다. `Settings`에 필드가 없어서 지금은 코드를 고쳐야만 조절할 수 있습니다.

### 확인

위 재현 스크립트를 다시 돌려 `distinct providers`가 `1`이 나오면 됩니다.

```bash
uv run pytest -o addopts="" tests/retrieval/test_07_service.py -q
```

### 정리

`retrieve()`의 provider 기본 경로가 호출마다 새 인스턴스와 새 HTTP 풀을 만들고 있었습니다. 지금은 모든 호출부가 provider를 넘겨서 발현하지 않지만, M4의 서비스 계층은 반드시 기본 경로를 밟습니다. 캐싱이든 필수 인자화든, **한 프로세스에 provider 하나**라는 불변조건만 세워두면 됩니다.

---

## 4. 초록불이 거짓말을 하고 있다

### 증상

`make typecheck`가 통과합니다. `make test`도 통과합니다. 그런데 둘 다 실제로는 검사하지 않고 있습니다. 그리고 `make docs`는 **지금 실제로 실패하고 있습니다.**

### 4-1. `make typecheck`의 초록은 가짜입니다

`pyproject.toml:183-185`를 보시죠.

```toml
# M3+ 테스트는 대응하는 app 모듈이 zero에 아직 없어 임시 제외한다.
# 각 모듈을 구현하는 시점에 해당 항목을 지운다.
exclude = ["tests/agent", "tests/api", "tests/demo", "tests/evals", "tests/release", "tests/workflow"]
```

주석이 규칙을 명확히 적어 두었습니다. **"각 모듈을 구현하는 시점에 해당 항목을 지운다."** `app/evals`는 2,927줄로 이미 완성되었습니다. 그런데 `tests/evals`가 아직 제외 목록에 남아 있습니다.

제외를 풀면 실제 오류 4건이 드러납니다.

```text
tests/evals/test_06_postgres.py:152 - "str" is not assignable to Literal['lexical'|'vector'|'hybrid']
tests/evals/test_06_postgres.py:154 - "str | None" is not assignable to "LexicalRanker | None"
tests/evals/test_03_regression.py   - SQLAlchemy 반영 접근에 대한 속성 오류 2건
```

여기서 함정을 하나 알려드립니다. 확인해 보시겠다고 이렇게 실행하면 **속습니다.**

```bash
uv run --with basedpyright basedpyright tests/evals
```

이 명령은 `0 errors`를 냅니다. 그런데 `--outputjson`으로 보면 `filesAnalyzed: 0`입니다. 명령줄에 경로를 줘도 `exclude`가 여전히 적용되어 **아무 파일도 검사하지 않은 것**입니다. 검사한 파일이 0개면 오류도 0개입니다. 제외 항목을 실제로 지운 뒤 확인하셔야 합니다.

### 4-2. `make docs`는 지금 실패 중입니다

```text
docs/en/m3-evals/tutorial/05-ablation.md: paragraph is hard wrapped
docs/en/m3-evals/tutorial/06-evaluation-records.md: paragraph is hard wrapped
docs/en/m3-evals/tutorial/07-evaluation-run.md: paragraph is hard wrapped
docs/ko/m3-evals/tutorial/05-ablation.md: paragraph is hard wrapped
docs/ko/m3-evals/tutorial/06-evaluation-records.md: paragraph is hard wrapped
docs/ko/m3-evals/tutorial/07-evaluation-run.md: paragraph is hard wrapped
Format check: 6 of 285 file(s) need reflow.
```

해결은 명령 한 줄입니다.

```bash
uv run python scripts/format_docs.py docs
```

### 4-3. 기본 테스트 게이트가 M2와 M3를 한 줄도 실행하지 않습니다

`pyproject.toml:93-108`의 `testpaths`에는 M1.1 시절 파일 11개만 들어 있습니다. `uv run pytest`는 333개를 수집하는데, 그 333개가 `app/db`, `app/retrieval`, `app/evals` 중 어디에도 닿지 않습니다.

커버리지로 재보면 명확합니다.

```text
app/evals/retrieval_eval.py   376/376 missed   0%
app/ingestion/seed.py         261/261 missed   0%
app/evals/ablation.py                          0%
app/evals/scoring.py                           0%
app/retrieval/service.py                       0%
app/retrieval/bm25.py                          0%
```

**가장 최근 커밋이 고친 코드를 기본 게이트가 전혀 실행하지 않습니다.** 이것은 pyproject 주석에 기록된 의도적 정책이긴 합니다. 하지만 M3까지 완성한 지금은 정책의 전제가 사라졌습니다.

거기에 하나가 더 겹칩니다. 아래 명령을 실행해 보세요.

```bash
RETRIEVAL_BM25_MODULE=app.retrieval.__missing__ uv run pytest -o addopts="" -q -p no:randomly tests/retrieval/test_09_bm25.py
```

```text
18 passed, 33 skipped in 0.80s      (정상일 때: 51 passed)
```

**BM25 모듈이 통째로 없는데 18개가 통과합니다.** 실패가 아니라 skip이 나오기 때문입니다.

> **개념 — `need()`와 `optional_module()`이 하는 일, 그리고 그 대가**
>
> `tests/support.py:12-16`의 `need()`는 모듈에 심볼이 없으면 `pytest.skip()`을 호출합니다. `optional_module()`은 import 자체가 실패해도 빈 모듈을 돌려줍니다.
>
> 이것은 학습 브랜치를 위한 합리적인 설계입니다. 아직 구현하지 않은 체크포인트의 테스트가 빨간불로 뜨면 진짜 실패와 구분이 안 되니까요. 구현 전에는 조용히 넘어가고, 구현하면 그때부터 검사가 시작됩니다.
>
> 대가는 이것입니다. **모듈이 사라지거나 망가져도 실패가 아니라 skip이 됩니다.** 구현이 끝난 모듈에 대해서는 이 안전장치가 오히려 안전망을 뚫는 구멍이 됩니다.

BM25 테스트에는 또 하나 짚을 점이 있습니다. 51개 중 10개는 앱 코드를 아예 호출하지 않고, 파이썬으로 다시 쓴 참조 구현을 JSON 픽스처와 비교할 뿐입니다. 게다가 SQL 문자열을 부분 문자열로 검사하는 테스트들은 **BM25 상수가 bind parameter라서 SQL 텍스트에 나타나지 않습니다.** 즉 `k1`이나 `b`를 잘못 넣어도 오프라인 테스트는 잡지 못합니다.

레이턴시 지표도 비슷합니다. `_latency_summary`에서 `p95`를 `max`로, `p50`을 `min`으로 바꿔치기하는 변이 테스트를 돌려도 **116개가 그대로 통과합니다.** `p95_ms`는 두 곳에서만 검증되는데 둘 다 값이 퇴화된 입력(`[1.0, 3.0]`, 동일값 200개)이고, `p50_ms`와 `max_ms`는 검증이 아예 0건입니다.

### 수정

세 가지를 하시면 됩니다.

**하나**, `pyproject.toml:185`에서 `"tests/evals"`를 지우고 드러나는 오류 4건을 고칩니다. 대부분 문자열을 `Literal` 타입으로 좁히거나 `cast`하는 작업입니다.

**둘**, `uv run python scripts/format_docs.py docs`로 6개 파일을 정리합니다.

**셋**, 게이트에 M2/M3를 넣습니다. `testpaths`를 넓히거나, `Makefile`의 `quality` 타깃에 해당 스위트를 추가하는 방법이 있습니다. 학습 흐름상 `make test`의 가벼움을 유지하고 싶으시면 후자를 권합니다. `make quality`는 어차피 "이제 됐다"를 확인하는 자리니까요.

### 확인

```bash
make lint && make typecheck && make docs && make test
```

### 정리

타입 검사는 완성된 모듈의 테스트를 제외하고 있었고, 문서 게이트는 실제로 실패 중이었으며, 기본 테스트는 M2와 M3 코드에 전혀 닿지 않고 있었습니다. 게다가 `need()` 기반 skip 때문에 모듈이 없어도 실패가 아니라 skip이 납니다. **초록불을 믿을 수 있게 만드는 것이 이 항목의 목표입니다.** 앞으로 재인덱싱과 청커 수정처럼 파급이 큰 작업을 할 텐데, 그 전에 안전망이 있어야 합니다.

---

## 5. 테이블 인용이 46KB짜리인 이유

### 이 항목의 성격을 먼저 말씀드립니다

항목 1에서 채점 규칙을 고쳐 지표 천장을 없앴습니다. **이 항목은 지표를 위한 것이 아닙니다.** 인용 품질을 위한 것입니다.

M4는 답변을 만들면서 근거를 인용합니다. 사용자에게 "이 표 전체(46KB)를 보세요"라고 하는 것과 "이 표의 3~5행을 보세요"라고 하는 것은 완전히 다른 경험입니다. 그리고 근거 span은 M4가 LLM에게 무엇을 보여줄지 결정하는 데도 쓰입니다.

> **개념 — provenance, 그리고 인용 정밀도가 RAG에서 왜 중요한가**
>
> provenance는 "이 데이터가 어디서 왔는가"를 추적할 수 있게 하는 정보입니다. 우리 시스템에서는 `(source_sha256, start_char, end_char)` 세 값이 그 역할을 합니다. 어떤 문서 스냅샷의 어느 구간에서 이 청크가 나왔는지를 정확히 가리킵니다.
>
> RAG 시스템에서 이게 왜 중요하냐면, **LLM이 지어낸 답과 실제 근거를 구분할 수 있는 유일한 수단**이기 때문입니다. 답변에 붙은 인용을 따라가서 원문에서 그 문장을 확인할 수 있어야 합니다. 인용이 46KB짜리 표 전체를 가리키면 검증이 사실상 불가능해집니다. 사용자가 46KB를 다 읽어야 하니까요.
>
> SEC 10-K 리뷰처럼 숫자가 중요한 도메인에서는 특히 그렇습니다. "매출이 609억 달러"라는 답변의 근거가 표 전체라면, 그 숫자가 어느 칸에서 나왔는지 아무도 모릅니다.

### 증상과 규모

```text
kind    count   span p50   span p90   body p50   span/body p50
text    8,083      1,403      4,212        781            1.90
table   1,089     13,617     46,631        401           31.93
```

테이블 청크는 전체의 11.9%에 불과한데, **인용된 원문 문자의 49.3%를 차지합니다.** 2,190만 자를 인용해서 67.5만 자의 본문을 전달하고 있습니다. 32.4배입니다.

### 여기에 잘라낼 여유분이 있는가 — 없습니다

먼저 이 질문부터 해결해야 합니다. span이 필요 이상으로 넓게 잡혀 있어서 그냥 좀 줄이면 되는 것 아닐까요?

확인해 봤습니다. MU-FY2020에서 가장 넓은 테이블 span(157,492자)의 끝부분을 원문에서 꺼내 보면 이렇습니다.

```text
...</td></tr></table></ix:nonNumeric></div><div style="text-indent:-18pt;padding-left:18pt;">...
```

`end_char`가 `</table>` 뒤 약 30자 지점, 바로 다음 `<div>`가 시작하는 자리에 정확히 떨어집니다. NVDA-FY2024의 테이블 47개를 조사해 보니 `</table>` 이후 여유분이 중앙값 22자, 최대 360자였습니다.

**즉 span은 이미 테이블 마크업 그 자체입니다.** 넓게 잡힌 게 아니라 테이블이 원래 그만큼 큽니다. 좁히려면 테이블 안쪽, 즉 행 단위로 내려가는 수밖에 없습니다.

### 원인 — 오프셋이 어디서 사라지는가

행 오프셋이 아예 없는 것은 아닙니다. 트리에는 있습니다. NVDA-FY2024의 첫 테이블을 조사한 결과입니다.

```text
table sourceline/pos: 5 122555 -> abs 123488
  tr: 5 122675 -> abs 123608 ; first td abs: 123612
  tr: 5 122833 -> abs 123766 ; first td abs: 123770
```

행마다 절대 오프셋이 나옵니다. 문제는 이 정보가 파이프라인을 타고 내려오면서 **세 지점에서 버려진다**는 것입니다.

> **개념 — 소스 오프셋 맵과 `sourceline` / `sourcepos`**
>
> BeautifulSoup을 `store_line_numbers=True`로 만들면(`parser.py:268-269`) 각 태그가 원본에서 몇 번째 줄, 그 줄의 몇 번째 칸에서 시작했는지를 기억합니다. 이것이 `sourceline`과 `sourcepos`입니다.
>
> 줄/칸 좌표를 절대 위치로 바꾸려면 줄 시작 오프셋 표가 필요합니다. `parser.py:224-228`의 `line_offsets`가 원본 문자열을 한 번 훑어서 그 표를 만듭니다. 그 뒤 `source_pos`(`:231-248`)가 `offsets[sourceline - 1] + sourcepos`로 절대 위치를 계산합니다.
>
> 여기서 중요한 제약이 하나 있습니다. **이 방식은 시작 위치만 알려줍니다.** 태그가 어디서 끝나는지는 기록되지 않습니다.

> **개념 — 끝 위치가 없어서 생긴 "타일링" 방식**
>
> 끝 위치를 모르는데 span을 만들어야 하니, `block_source_spans`(`parser.py:573-581`)는 영리한 우회를 씁니다. **다음 블록의 시작을 내 끝으로 삼습니다.** 블록 목록을 거꾸로 훑으면서 직전에 본 시작 위치를 현재 블록의 끝으로 채워 넣고, 마지막 블록은 문서 전체 길이를 끝으로 받습니다.
>
> 결과적으로 블록들이 문서를 **빈틈없이 타일처럼 덮습니다.** 어떤 문자도 두 블록에 속하지 않고, 어떤 문자도 빠지지 않습니다. 이 성질이 지금 코드 여러 곳에서 암묵적으로 쓰이고 있습니다. 항목 5의 가장 큰 난관이 바로 여기서 나옵니다.

이제 정보가 사라지는 세 지점입니다.

| 지점 | 코드 | 잃는 것 |
|---|---|---|
| `parser.py:614-620` `_body_block` | `html=str(el)` | Tag를 문자열로 직렬화해 모든 자손이 오프셋 맵과 끊어집니다 |
| `tables.py:353-355` `table_to_markdown` | `BeautifulSoup(html, "html.parser")` | `store_line_numbers` 없이 재파싱해서 `tr.sourceline`이 전부 `None`이 됩니다 |
| `tables.py:5` `Grid = list[list[str]]` | 문자열 격자 | 이후 변환들이 행과 열을 지우고 재배치하는데 원본 행 인덱스가 없습니다 |

세 번째가 특히 성가십니다. `drop_empty`(`tables.py:113-131`)가 빈 행과 빈 열을 지우고, `merge_unit_columns`(`:133-201`)가 열을 합치면서 텍스트를 **열 사이로 옮기고**, `split_header`(`:203-241`)가 행을 다시 자릅니다. 최종 마크다운의 어느 줄이 원본의 어느 `<tr>`에서 왔는지 추적할 방법이 없습니다.

### 불변조건

- 테이블 청크의 `[start_char, end_char)`는 그 본문을 만든 행들만 덮어야 합니다.
- DB 제약 `start_char >= 0`과 `end_char > start_char`는 계속 성립해야 합니다. span 폭에 대한 제약은 없으니 좁히는 것 자체는 안전합니다.
- `seed.py`의 검증 세 곳(`:169-170`, `:241-242`, `:356-357`)도 계속 통과해야 합니다.
- **골든 파일은 손대지 않습니다.** 정답 좌표는 원문 기준이라 청크 span이 좁아져도 그대로 유효합니다.

### 수정 — 네 단계

**1단계.** `Block`(`parser.py:33-43`)에 행 provenance를 담을 필드를 추가합니다. 지금은 `source_pos`와 `end_pos` 한 쌍만 있습니다.

**2단계.** `_body_block`에서 `str(el)`을 호출하기 **전에** 행 span을 계산합니다. `table.find_all("tr", recursive=False)`로 직계 행을 모으고, `block_source_spans`가 쓰는 것과 똑같은 next-sibling 방식을 적용하면 됩니다. 마지막 행의 끝은 테이블 블록의 `end_pos`로 받으면 자연스럽습니다.

**3단계.** `Grid`에 원본 행 인덱스를 나란히 보존합니다. `drop_empty`, `merge_unit_columns`, `split_header`를 전부 통과해야 합니다. 여기가 가장 손이 많이 가는 부분입니다.

**4단계.** `section_units`(`chunk.py:172-177`)가 블록 봉투 대신 살아남은 행들의 최소/최대를 쓰도록 바꿉니다.

헤더 처리에 주의하셔야 합니다. `to_markdown`(`tables.py:275-280`)이 여러 헤더 행을 열 단위로 병합하고, `merge_unit_columns`는 텍스트를 열 사이로 옮깁니다. 그래서 최종 span은 행들의 집합이 아니라 **기여한 행들을 모두 포함하는 연속 구간(hull)**이어야 합니다. 중간에 기여하지 않은 행이 끼어 있어도 그것까지 포함해야 연속성이 유지됩니다.

### 막고 있는 테스트 두 개

**`tests/chunk/test_05_roundtrip.py:69-72`가 정면으로 막습니다.**

```python
chunk.start_char <= block.source_pos and block.end_pos <= chunk.end_char
```

블록 하나가 정확히 청크 하나에 담기는지를 확인합니다. 그런데 앞서 본 것처럼 블록의 `end_pos`는 **다음 블록의 시작**입니다. 테이블 span을 행 부분집합으로 좁히면 자기 블록의 `end_pos`를 더 이상 포함하지 못합니다. **모든 테이블에서 깨집니다.**

이 테스트는 원래 "청크가 블록을 온전히 담는다"는 좋은 성질을 지키고 있었습니다. 새 성질로 다시 쓰셔야 합니다. 예를 들어 "테이블 청크의 span은 그 테이블 블록의 `[source_pos, end_pos)` 안에 들어 있어야 한다"처럼 방향을 뒤집는 것이 자연스럽습니다.

**`tests/chunk/test_04_tables.py:7`은 이름 자체가 신호입니다.**

```python
def test_table_is_not_split_without_row_level_provenance():
```

현재 동작이 우연이 아니라 **계약**이라는 뜻이고, 그 이름이 지금 없는 바로 그것(행 단위 provenance)을 가리키고 있습니다. 저자가 이 한계를 알고 이름에 새겨 둔 것입니다. 행 provenance를 만들면 이 테스트의 전제가 사라지므로 다시 써야 합니다. 하드코딩된 `(10, 200)`도 함께 손봐야 합니다.

### 파급

`start_char` / `end_char`를 소비하는 곳이 많습니다. 주요한 것만 짚습니다.

- 정렬과 동점 처리: `chunk.py:260`의 ordinal 재부여, `lexical.py:128-129`의 `hit_order_by`(모든 검색 전략이 공유)
- 검증: `retrieval/types.py:33-34,44-45`, `seed.py` 세 곳
- 평가: `evals/loader.py:258-264`(정답 span의 원문이 비어 있지 않은지 확인), `scoring.py`
- 테스트: `tests/chunk`, `tests/db`, `tests/retrieval`, `tests/evals` 전반에 span 리터럴이 흩어져 있습니다

`tests/chunk/test_03_text.py:141-151`이 텍스트 span들이 서로 겹치지 않는지 확인하는데, 텍스트 블록은 건드리지 않으므로 무사합니다.

### 확인

```bash
uv run pytest -o addopts="" tests/chunk tests/ingestion -q
```

그리고 span이 실제로 좁아졌는지 재보세요.

```bash
uv run python -c "
import asyncio
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
from app.config import get_settings
async def main():
    e = create_async_engine(get_settings().database_url)
    async with e.connect() as c:
        r = await c.execute(text(\"select kind, count(*), percentile_cont(0.5) within group (order by end_char-start_char)::int span_p50, percentile_cont(0.5) within group (order by length(body))::int body_p50 from chunks group by kind order by kind\"))
        for row in r: print(row)
    await e.dispose()
asyncio.run(main())
"
```

**테이블의 `span_p50`이 13,617에서 크게 내려가야 합니다.**

### 정리

테이블 청크는 본문 401자를 전달하려고 원문 13,617자를 인용하고 있었습니다. span에 여유분이 있어서가 아니라 테이블 마크업 자체가 그만큼 크기 때문이고, 좁히려면 행 단위 provenance를 파이프라인에 되살려야 합니다. 오프셋은 트리에 존재하지만 직렬화·재파싱·문자열 격자 세 지점에서 버려집니다. 이 작업은 지표를 위한 것이 아니라 **M4가 정밀하게 인용할 수 있게 하기 위한 것**입니다.

---

## 6. 재인덱싱 사이클 — 가짜 임베딩을 진짜로 바꾸기

### 증상

지금 데이터베이스에 들어 있는 벡터 9,172개가 **전부 의미 없는 해시값**입니다.

### 재현

```bash
uv run python -c "
import asyncio, numpy as np
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
from app.config import get_settings
from app.retrieval.embeddings import DeterministicEmbeddingProvider
async def main():
    e = create_async_engine(get_settings().database_url)
    async with e.connect() as c:
        rows = (await c.execute(text('select id, index_text, embedding::text from chunks order by random() limit 5'))).fetchall()
    await e.dispose()
    det = DeterministicEmbeddingProvider(384)
    for cid, txt, vec in rows:
        stored = np.array([float(x) for x in vec.strip('[]').split(',')])
        got = np.array((await det.embed_documents([txt]))[0])
        cos = float(stored @ got / (np.linalg.norm(stored) * np.linalg.norm(got)))
        print(f'chunk {cid}: cosine(stored, deterministic) = {cos:.12f}  zeros={int((stored==0).sum())}/384')
asyncio.run(main())
"
```

```text
chunk 1499: cosine(stored, deterministic) = 1.000000000000  zeros=346/384
chunk 7819: cosine(stored, deterministic) = 1.000000000000  zeros=349/384
chunk 3214: cosine(stored, deterministic) = 1.000000000000  zeros=341/384
```

코사인 유사도가 **소수점 12자리까지 정확히 1.0**입니다. 저장된 벡터가 `DeterministicEmbeddingProvider`의 출력과 완전히 같다는 뜻입니다.

그 클래스의 docstring이 스스로 무엇인지 말하고 있습니다.

```python
class DeterministicEmbeddingProvider(EmbeddingProvider):
    """Stable token-hashing vectors for tests and local exercises."""
```

**"테스트와 로컬 연습용"**입니다. 토큰을 SHA-256으로 해싱해서 차원 인덱스를 뽑고 부호를 정하는 방식이라, 의미론이 전혀 없습니다. 384차원 중 평균 346개가 정확히 0인 것도 그래서입니다.

그 결과가 벡터 검색 성적입니다.

```text
vector arm recall@5 = 0.0625
```

### 왜 이게 지금 문제인가

`app/config.py:26`의 기본값을 보시죠.

```python
embedding_provider: EmbeddingProviderName = "openai"
```

그리고 `.env`에는 `EMBEDDING_PROVIDER`가 없습니다. 즉 **기본 설정으로 검색하면 OpenAI로 쿼리를 임베딩해서 해시 벡터와 비교합니다.**

> **개념 — 임베딩 공간과 왜 provider를 섞으면 안 되는가**
>
> 임베딩 모델은 텍스트를 벡터로 바꿉니다. 이때 벡터의 각 차원에는 사람이 이해할 수 있는 의미가 없습니다. 의미는 **벡터들 사이의 상대적 위치**에만 있습니다. "고양이"와 "강아지"가 가깝고 "고양이"와 "회계감사"가 멀다는 관계가 전부입니다.
>
> 이 관계는 모델마다 완전히 다르게 배치됩니다. OpenAI가 만든 공간에서의 좌표와 MiniLM이 만든 공간에서의 좌표는 아무 상관이 없습니다. **같은 384차원이어도 다른 공간입니다.** 서로 다른 지도에서 뽑은 위경도를 비교하는 것과 같습니다.
>
> 무서운 점은 이게 오류를 내지 않는다는 것입니다. 차원 수만 맞으면 코사인 유사도가 계산되고, 정렬되고, 상위 5개가 나옵니다. 그냥 그 5개가 무의미할 뿐입니다. `app/retrieval/sbert.py:21-24`의 docstring이 정확히 이렇게 경고하고 있습니다. "Mixing them yields no error, only meaningless neighbours."

여기에 설계상의 함정이 겹칩니다. `service.py:165`에 가드가 하나 있긴 합니다.

```python
if active_provider.dimensions != DIM:
    raise ValueError(...)
```

그런데 `app/config.py:29`가 `embed_dim: Literal[384] = 384`입니다. 그리고 세 provider가 전부 384차원입니다. **그러니 이 가드는 어떤 설정으로도 발화할 수 없습니다.** 유일하게 걸리는 경우는 테스트가 손으로 32차원 provider를 만들어 넣을 때뿐입니다.

### 재임베딩 경로가 없습니다

provider를 바꾸고 다시 임베딩하려고 하면 벽에 부딪힙니다. `app/retrieval/embeddings.py:270`을 보시죠.

```python
.where(Chunk.embedding.is_(None))
```

`embed_missing_chunks`는 **비어 있는 것만** 채웁니다. 지금 9,172개가 전부 채워져 있으니 0개를 선택하고, "0개 임베딩 완료"를 보고하며 성공 종료합니다.

CLI 도움말(`__main__.py:38-42`)은 이 상황을 알고 있습니다.

> switching requires re-embedding every chunk with `--embed-missing` on an empty column

**"컬럼을 비운 상태에서"** 하라고 합니다. 그런데 컬럼을 비우는 명령이 없습니다. 직접 만드셔야 합니다.

### 다행인 점 하나

`chunk_upsert_statement`(`seed.py:670-673`)에는 아주 잘 만들어진 장치가 있습니다.

```python
"embedding": case(
    (ChunkModel.index_text == excluded.index_text, ChunkModel.embedding),
    else_=None,
),
```

재시드할 때 `index_text`가 그대로면 임베딩을 보존하고, 바뀌었으면 `NULL`로 비웁니다. 비자명하고 정확한 최적화입니다. **항목 5 작업 중에 이 로직을 반드시 보존하세요.**

그런데 이번에는 이 장치가 오히려 도움이 됩니다. 항목 5로 테이블 청크의 span이 바뀌면 `index_text`도 바뀔 가능성이 높고, 그러면 해당 청크들의 임베딩이 자동으로 비워집니다. 다만 텍스트 청크는 그대로일 테니 **전체 재임베딩을 하려면 명시적으로 컬럼을 비우는 절차가 여전히 필요합니다.**

### 수정

**하나, 재임베딩 경로를 만듭니다.** `--reembed-all` 같은 플래그로 임베딩 컬럼을 비우고 백필을 이어서 돌리는 형태가 자연스럽습니다. 되돌릴 수 없는 작업이니 확인 절차를 두시는 편이 좋습니다.

**둘, ablation에 `sbert`를 추가합니다.** `app/evals/retrieval_eval.py:1210`이 지금 이렇습니다.

```python
parser.add_argument("--provider", choices=("deterministic", "openai"), default="deterministic")
```

`sbert`는 `EmbeddingProviderName`(`config.py:12`)에 있는 정당한 값인데 여기서 빠져 있습니다. **그래서 무료 semantic arm을 코드 수정 없이는 측정할 수 없습니다.** `choices` 튜플에 한 항목 추가하면 됩니다.

**셋, provider 정체성을 관측 가능하게 만듭니다.** 최소한 임베딩을 채운 provider가 무엇인지 어딘가에 남기세요. 지금은 우연히 deterministic이라 재계산으로 알아낼 수 있었지만, sbert나 OpenAI 벡터였다면 알아낼 방법이 없습니다.

### 실행 순서 — 이 순서를 지키셔야 합니다

```bash
uv run python -m app.ingestion.seed --create-schema
```

```bash
uv run python -m app.retrieval --query "warmup" --rebuild-bm25-stats --provider deterministic --k 1
```

```bash
EMBEDDING_PROVIDER=sbert uv run python -m app.retrieval --query "warmup" --embed-missing --k 1
```

> **개념 — 파생 인덱스와 배치 재빌드 패턴**
>
> 이 프로젝트는 임베딩과 BM25 통계를 **시딩과 분리된 배치 백필**로 둡니다. 처음 보면 이상하게 느껴질 수 있습니다. 청크를 넣을 때 같이 채우면 되지 않나요?
>
> 이유는 성격이 다르기 때문입니다. 청크 자체는 원문에서 결정론적으로 나오지만, 임베딩은 외부 API 호출이나 GPU 연산이 필요하고 실패할 수 있습니다. BM25 통계는 더 근본적인 이유가 있습니다. `lexeme_stats.df`는 **"이 단어를 포함한 청크가 코퍼스 전체에서 몇 개인가"**라는 전역 값입니다. 문서 하나를 재시드했다고 그 문서만 갱신할 수 있는 값이 아닙니다. 전체를 다시 세야 합니다.
>
> 그래서 `backfill_term_stats`는 원자적 전체 재빌드로 설계되어 있습니다. 이건 결함이 아니라 올바른 구조입니다. 다만 **재시드 후에 반드시 돌려야 한다**는 것을 알고 계셔야 합니다. `seed.py`의 어느 경로도 이것을 자동으로 부르지 않습니다.

참고로 지금 라이브 DB의 BM25 통계는 **완벽하게 정합합니다.** 직접 확인한 결과입니다.

```text
chunks w/o chunk_lengths       -> 0
orphan chunk_lengths           -> 0
dl mismatch vs sum(tf)         -> 0
df mismatch                    -> 0
lexemes missing from stats     -> 0
stale stats lexemes            -> 0
avgdl                          -> 82.289
```

재시드가 이 정합을 깹니다. 그래서 재빌드가 필수입니다.

> **개념 — BM25의 네 손잡이**
>
> 재빌드 후 검색 품질이 달라지면 이 값들을 의심하게 되니 미리 정리해 둡니다. `app/retrieval/bm25.py:186-190`의 식은 교과서 그대로입니다.
>
> `tf`는 이 청크에 그 단어가 몇 번 나왔는가입니다. 많이 나올수록 관련성이 높지만 선형으로 높아지진 않습니다. `k1`이 그 포화 속도를 정합니다. 값이 클수록 반복의 효과가 오래 유지됩니다. 기본값 1.2입니다.
>
> `dl`은 이 청크의 길이, `avgdl`은 코퍼스 평균 길이입니다. 긴 문서는 아무 단어나 우연히 포함하기 쉬우므로 벌점을 줍니다. `b`가 그 벌점의 강도이고, 0이면 길이를 무시, 1이면 완전히 반영합니다. 기본값 0.75입니다.
>
> `df`는 그 단어를 포함한 청크 수, `N`은 전체 청크 수입니다. 흔한 단어일수록 변별력이 없으니 가중치를 낮춥니다. 이것이 IDF입니다. `lucene` 변종은 로그 앞에 1을 더해 IDF가 음수로 내려가지 않게 막고, `robertson` 변종은 아주 흔한 단어에 음수 가중치를 줄 수 있습니다.
>
> 우리 코퍼스의 `avgdl`이 82.289인데, 이건 lexeme 개수 기준입니다. 페이지 번호 같은 짧은 청크가 이 평균을 끌어내리고 있어서 정상 길이 청크가 상대적으로 더 큰 길이 벌점을 받고 있습니다.

### 확인

```bash
uv run python -c "
import asyncio, numpy as np
from sqlalchemy import text
from sqlalchemy.ext.asyncio import create_async_engine
from app.config import get_settings
from app.retrieval.embeddings import DeterministicEmbeddingProvider
async def main():
    e = create_async_engine(get_settings().database_url)
    async with e.connect() as c:
        rows = (await c.execute(text('select id, index_text, embedding::text from chunks order by random() limit 3'))).fetchall()
        print('null embeddings:', (await c.execute(text('select count(*) from chunks where embedding is null'))).scalar())
    await e.dispose()
    det = DeterministicEmbeddingProvider(384)
    for cid, txt, vec in rows:
        stored = np.array([float(x) for x in vec.strip('[]').split(',')])
        got = np.array((await det.embed_documents([txt]))[0])
        print(f'chunk {cid}: cos vs deterministic = {float(stored @ got / (np.linalg.norm(stored) * np.linalg.norm(got))):.6f}  zeros={int((stored==0).sum())}/384')
asyncio.run(main())
"
```

**이제 코사인이 1.0에서 멀어지고, 0인 성분이 급감해야 합니다.** sbert 벡터는 조밀하므로 정확히 0인 성분이 거의 없어야 정상입니다.

### 정리

라이브 코퍼스의 벡터 9,172개가 전부 테스트용 해시값이었고, 기본 설정은 OpenAI를 가리키고 있어서 서로 다른 공간의 벡터를 비교할 참이었습니다. 재임베딩 경로를 새로 만들고, ablation에 sbert를 열고, 재시드 → BM25 재빌드 → 재임베딩 순서로 한 사이클을 돌렸습니다. 이제 vector arm이 처음으로 의미 있는 숫자를 낼 수 있습니다.

---

## 7. 재검증 — 그리고 숫자를 정직하게 읽는 법

### 순서

**1단계, 채점 천장이 사라졌는지 확인합니다.** 항목 1의 목표가 달성됐는지 보는 자리입니다.

```bash
uv run python -c "
import asyncio, json
from sqlalchemy import text as sql
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
from app.config import get_settings
from app.evals.scoring import span_iou, IOU_THRESHOLD
from app.evals.types import GoldenSpan
g = json.load(open('data/golden/retrieval.json')); pos = [c for c in g if c['answers']]
class H: pass
async def main():
    e = create_async_engine(get_settings().database_url); S = async_sessionmaker(e, expire_on_commit=False)
    async with S() as s:
        zero = []
        for c in pos:
            reach = 0
            for a in c['answers']:
                rows = (await s.execute(sql('select start_char,end_char from chunks where doc_id=:d and source_sha256=:h and start_char<:e and end_char>:st'), {'d': a['doc_id'], 'h': a['source_sha256'], 'st': a['start_char'], 'e': a['end_char']})).fetchall()
                gs = GoldenSpan(**a); mx = 0.0
                for r in rows:
                    h = H(); h.doc_id = a['doc_id']; h.source_sha256 = a['source_sha256']; h.start_char = r.start_char; h.end_char = r.end_char
                    mx = max(mx, span_iou(gs, h))
                if mx >= IOU_THRESHOLD: reach += 1
            if reach == 0: zero.append(c['id'])
        print('cases with ceiling 0:', len(zero), zero)
    await e.dispose()
asyncio.run(main())
"
```

**`0 []`가 나와야 합니다.** 이름은 아직 `span_iou`지만 항목 1에서 바꾸신 이름으로 맞춰 주세요.

**2단계, 라이브 코퍼스 골든 성적을 다시 잽니다.** 현재 기준선은 이렇습니다.

```text
bm25         recall@5=0.5208  hit@5=0.5417  mrr=0.3951
ts_rank_cd   recall@5=0.2708  hit@5=0.2917  mrr=0.1715
vector(det)  recall@5=0.0625  hit@5=0.0833  mrr=0.0833
```

**3단계, ablation을 다시 돌립니다.** sbert와 openai arm을 포함해서요.

**4단계, 전체 게이트를 통과시킵니다.**

```bash
make lint && make typecheck && make docs && make test
```

### 숫자를 읽을 때 반드시 아셔야 할 것

여기가 이 문서에서 두 번째로 중요한 부분입니다. 재실행해서 나온 숫자를 어떻게 해석하느냐의 문제입니다.

> **개념 — 통계적 유의성, 그리고 McNemar 검정**
>
> arm A가 recall 0.52이고 arm B가 0.46이면 A가 더 좋은 걸까요? 24개 케이스로 잰 값이라면 함부로 그렇게 말할 수 없습니다. 케이스 한두 개가 우연히 다르게 나왔을 뿐일 수도 있으니까요.
>
> 두 arm을 **같은 케이스 집합**에 대해 비교할 때 쓰는 검정이 McNemar 검정입니다. 핵심 아이디어는 이렇습니다. 둘 다 맞힌 케이스와 둘 다 틀린 케이스는 정보가 없으니 버립니다. **의견이 갈린 케이스(discordant pair)만** 봅니다.
>
> A만 맞힌 게 6개이고 B만 맞힌 게 0개라면, 동전 던지기로 이런 일이 생길 확률은 `2 * (1/2)^6 = 0.03125`입니다. 충분히 낮으니 우연이 아니라고 봅니다. 반면 A만 맞힌 게 2개, B만 맞힌 게 0개면 확률이 0.5입니다. 동전 두 번 던져 같은 면이 나온 것과 다르지 않습니다.

우리 데이터로 계산한 결과입니다.

| 비교 | A만 맞힘 | B만 맞힘 | p | 판정 |
|---|---|---|---|---|
| 1200자 vs 500자 (청크 크기) | 2 | 0 | **0.5** | **구분 불가** |
| lexical-bm25 vs hybrid-bm25 | 6 | 0 | 0.03125 | 유의 |
| bm25 vs ts_rank_cd | 6 | 0 | 0.03125 | 유의 |
| hybrid vs vector | 5 | 0 | 0.0625 | 구분 불가 |

**첫 줄을 주목해 주세요.** `docs/en/eval-report.md`에 "1200자 BM25 arm이 가장 강하다"는 해석이 실려 있습니다. 그런데 그 결론은 **24개 케이스 중 1.5개 차이**에 기대고 있고, McNemar로 재면 p = 0.5입니다. 동전 던지기와 구분되지 않습니다.

이건 코드의 결함이 아닙니다. 측정 자체는 정확했습니다. **해석이 스위트의 분해능을 넘어선 것**입니다. 앞서 항목 1에서 본 것처럼 이 지표의 최소 눈금은 `0.5 / 24 = 0.0208`입니다. 그런데 결과는 소수점 여섯 자리로 출력됩니다(`ablation.py:263-268`). 여섯 자리 중 다섯 자리가 노이즈입니다.

재실행 후에 보고서를 다시 쓰실 때, 청크 크기에 대해서는 **"이 스위트로는 구분되지 않는다"**고 쓰시는 것이 정확합니다. 검색 전략 차이(bm25 대 ts_rank_cd, lexical 대 hybrid)는 유의하므로 그대로 결론 내리셔도 됩니다.

> **개념 — ablation 설계의 제1원칙**
>
> 어블레이션은 한 번에 축 하나만 바꾸는 실험입니다. 두 축을 동시에 바꾸면 결과가 달라져도 어느 축이 그 변화를 만들었는지 특정할 수 없습니다.
>
> 이번 재실행에서 특히 조심하셔야 합니다. 우리는 지금 **채점 규칙과 임베딩 provider와 청크 span을 한꺼번에 바꿉니다.** 새 숫자와 옛 숫자를 나란히 놓고 "좋아졌다"고 말할 수 없습니다. 항목 1에서 `ExperimentConfig`에 채점 규칙을 넣은 것이 바로 이래서입니다. **config가 다르면 시스템이 아예 비교를 시도하지 않습니다.**
>
> 새 기준선은 새로 세우는 것이고, 옛 숫자는 역사로 남깁니다. 비교는 그다음 실행부터 의미가 있습니다.

### 옛 아티팩트는 재채점할 수 없습니다

`data/eval_runs/`에 있는 커밋된 JSON을 새 규칙으로 다시 채점해서 비교하고 싶으실 수 있습니다. **불가능합니다.**

파일에는 케이스별 `hits`가 들어 있어서 원리상 재채점이 가능해 보입니다. 하지만 `score` 블록과 최상위 `metrics` 블록이 이미 계산된 값으로 동결되어 있습니다. 부분적으로만 갱신하면 파일 안에서 앞뒤가 안 맞게 됩니다. 재실행이 정답입니다.

### 정리

재검증은 천장 확인 → 골든 재측정 → ablation 재실행 → 게이트 통과 순서입니다. 그리고 나온 숫자를 읽을 때는 24개 케이스 스위트의 분해능이 0.0208이라는 것, 청크 크기 비교는 이 스위트로 판정할 수 없다는 것을 기억하셔야 합니다. **측정의 정확도와 해석의 신중함은 다른 문제입니다.**

---

## 전역 개념 두 가지

각 항목에 흩어 놓기 애매하지만 이 시스템을 이해하는 데 중요한 것 두 가지를 여기 모았습니다.

> **개념 — RRF는 왜 점수를 버리고 순위만 쓰는가**
>
> `app/retrieval/hybrid.py:89`가 융합의 전부입니다.
>
> ```python
> contribution = 1.0 / (rrf_k + rank)
> ```
>
> 벡터 검색은 코사인 유사도를 냅니다. 0에서 1 사이의 값이고, 실무에서는 대개 0.3에서 0.8 사이에 몰립니다. `ts_rank_cd`는 cover density 점수를 냅니다. 상한이 없고 우리 코퍼스에서는 0.05에서 0.3 사이입니다. BM25는 또 다릅니다. IDF 합이라 11에서 20 사이가 나옵니다.
>
> **이 셋은 서로 비교할 수 있는 단위가 아닙니다.** 0.7이라는 코사인과 19.8이라는 BM25 점수 중 어느 쪽이 더 확신에 찬 것인지 말할 방법이 없습니다. 정규화를 시도할 수도 있지만, 분포가 질의마다 달라서 안정적이지 않습니다.
>
> RRF는 이 문제를 우회합니다. **점수를 버리고 순위만 씁니다.** 어느 리스트에서든 1등은 1등이고, 그 사실만으로 `1/(60+1)`을 기여합니다. 단위 문제가 사라집니다. `rrf_k = 60`은 순위 간 격차를 완만하게 만드는 상수로, 1등과 2등의 차이가 지나치게 크지 않도록 합니다.
>
> 대가는 강도 정보를 버린다는 것입니다. 압도적 1등과 간신히 1등을 구분하지 못합니다. 그래도 척도가 다른 리스트를 합칠 때는 이쪽이 훨씬 안전합니다. `app/retrieval/hybrid.py:78-79`의 주석이 정확히 이 이유를 적어 두었습니다.

> **개념 — bi-encoder와 cross-encoder는 역할이 다릅니다**
>
> 벡터 검색이 쓰는 임베딩 모델은 bi-encoder입니다. 질의와 문서를 **따로** 벡터로 만든 다음 나중에 비교합니다. 따로 만들기 때문에 문서 벡터를 미리 계산해 저장해 둘 수 있고, 그래서 9,172개 청크 전체를 훑는 것이 가능합니다.
>
> cross-encoder는 질의와 문서를 **하나의 입력으로 붙여서** 모델에 넣고 관련성 점수 하나를 받습니다. 둘이 모델 안에서 만나기 때문에 훨씬 정확합니다. 대신 후보 하나마다 forward pass가 한 번씩 필요합니다. 전체 코퍼스에 돌릴 수 없습니다.
>
> 그래서 순서가 정해집니다. bi-encoder로 전체에서 후보 20개를 싸게 추리고, cross-encoder로 그 20개만 비싸게 다시 정렬합니다. `app/retrieval/cross_encoder.py:15-27`의 docstring이 이 분업을 잘 설명해 두었습니다.
>
> 참고로 이 리랭커는 이미 구현되어 있고 torch와 sentence-transformers도 설치되어 있는데, **ablation에는 rerank arm이 없습니다.** 가장 큰 품질 지렛대가 한 번도 골든셋으로 측정되지 않았습니다. 이번 범위에는 넣지 않았지만 M4를 시작하기 전에 한 번 재보실 가치가 충분합니다.

---

## 부록 A — 고치지 않기로 한 것들

감사 과정에서 문제로 제기됐지만, 검증 결과 **결함이 아니라고 판정된** 항목들입니다. 근거를 함께 남깁니다. 나중에 같은 자리를 다시 파지 않으시도록 하기 위함입니다.

**부정 검색 반전.** `revenue -risk`가 985건 대신 7,278건을 매치하는 것은 사실입니다. 하지만 커밋 `caddc70`과 출하 문서 세 곳이 "One semantic is knowingly weakened"라고 명시한 의도적 트레이드오프입니다. 골든 28개 중 `-`를 쓰는 질문이 하나도 없어 측정 영향도 0입니다.

**골든 승인 상태가 영원히 `pending`인 것.** `app/evals/types.py:56-58`이 `approval_status`와 `human_verified`를 리터럴로 못 박아서 승인된 상태를 표현할 수 없습니다. 이건 결함이 아니라 **의도적인 fail-closed 장치**입니다. 타입 docstring과 `data/golden/REVIEW.md`가 "저자만이 검토된 변경으로 이 값을 바꿀 수 있다"고 적어 두었습니다. 검토되지 않은 데이터가 승인된 것처럼 보이는 것을 원천 차단합니다.

**BM25 통계가 시딩 때 자동 갱신되지 않는 것.** 임베딩과 동일한, 문서화된 배치 재빌드 아키텍처입니다. `df`가 코퍼스 전역 값이라 부분 갱신이 불가능하기 때문에 올바른 설계입니다. 항목 6에 순서로만 기록했습니다.

**청크 골든이 순환 참조라는 지적.** `tests/chunk/golden.py`가 "Measured M1.3 corpus baseline"이라 자기 출력의 스냅샷처럼 보입니다. 그런데 git 이력을 보면 골든은 `aa23cc4`(2026-08-12)이고, `chunk.py`는 `ef84a67`(2026-08-13)에서 삭제된 뒤 `3e944c4`(2026-08-20)에 직접 재작성됐습니다. **골든이 현재 구현보다 8일 앞섭니다.** 직접 쓰신 청커가 그 수치를 재현해야 통과하므로, 순환이 아니라 오히려 강한 회귀 테스트입니다.

**기본 `lexical_ranker = "ts_rank_cd"`.** BM25가 더 좋은 성적을 내는 것은 사실이지만, `docs/en/eval-report.md:57`에 근거가 기록된, 언제든 되돌릴 수 있는 설정입니다.

**cross-encoder의 512토큰 제한.** 9,172개 청크 중 158개(1.7%)만 초과합니다. 선택한 모델의 고유한 한계이지 코드 결함이 아닙니다.

**엔진 미해제, `pool_pre_ping` 부재, cwd 상대경로 `.env`, 평가 러너의 프로덕션 DELETE, `detect_xref`의 미사용 파라미터.** 각각 개별 검증에서 반증됐습니다. 특히 프로덕션 DELETE 우려는 PostgreSQL이 "임시 테이블의 제약은 임시 테이블만 참조할 수 있다"는 규칙으로 구조적으로 차단합니다.

**근접 중복 제거와 문서별 상한이 없는 것.** 이건 사실 관계가 전부 맞습니다. 직접 재현했습니다.

```text
retrieve(k=5, 'goodwill impairment charges')  [deterministic provider]
  1. id=1535 AMD-FY2022  item=1A  score=0.030478
  2. id= 117 AMD-FY2019  item=1A  score=0.028629
  3. id=1707 AMD-FY2022  item=8   score=0.016393
  4. id=2314 INTC-FY2019 item=7   score=0.016129
  5. id=1105 AMD-FY2021  item=1A  score=0.015873

  per-doc counts: AMD 4, INTC 1
  NEAR-DUP 117 vs 1105 similarity = 1.000        <- 바이트 단위로 동일
  코퍼스 전체: total=9172  distinct=7882  exact-dup rows=1290
```

5개 슬롯 중 4개가 AMD로 갔고, 2번과 5번은 **완전히 같은 문단**입니다. AMD가 영업권 손상 회계정책 문구를 회계연도마다 그대로 반복하기 때문입니다. 코퍼스 전체로는 9,172개 중 1,290개(14.1%)가 다른 청크와 본문이 정확히 같습니다. 그리고 `app/` 어디에도 MMR, 근접 중복 제거, 문서별 상한이 존재하지 않습니다. `hybrid.py:24-43`의 `_unique_ranked_hits`는 **한 리스트 안에서 같은 `chunk_id`가 반복되는 경우만** 걸러내지, 서로 다른 `chunk_id`가 같은 텍스트를 담고 있는 것은 보지 못합니다. `service.py:232-238`은 융합 결과를 그대로 반환합니다.

**그런데 이것은 M2의 누락이 아니라 M4의 몫으로 명시된 경계입니다.** `docs/en/m4-workflow/tutorial/06-workflow-types.md:31`의 표가 코드가 강제해야 할 제약으로 이 둘을 직접 지목합니다.

| What the code does | What happens without it |
|---|---|
| deduplicate evidence | the same evidence enters several times, wasting tokens |
| cap the number of whole chunks | context overflow, cost blowup |

같은 문서 `:377`이 "It deduplicates and caps evidence"라고 되풀이합니다. 즉 증거 중복 제거와 상한은 검색 계층이 아니라 워크플로 계층의 책임으로 설계되어 있습니다. 문서화된 범위 결정이므로 이 문서에서 `app/retrieval/service.py`를 고치지 않습니다.

**다만 M4를 만드실 때 반드시 짚고 넘어가실 지점이 하나 있습니다.** M4 설계도 이 부분은 다루지 않습니다. `docs/en/m4-workflow/tutorial/08-runner.md:109`의 `make_session_retriever`를 보면 `k`를 `retrieve()`에 **그대로 넘깁니다.** 과다 인출(over-fetch)이 없습니다.

그러면 이렇게 됩니다. `retrieve(k=5)`가 5개를 돌려주는데 그중 2개가 동일하면, M4의 중복 제거는 그 둘을 하나로 합칩니다. 결과는 **증거 5개가 아니라 4개**입니다. 이미 잘라낸 목록에서 중복을 지우면 빈 슬롯을 되찾을 방법이 없습니다. 정석은 **넉넉히 인출 → 중복 제거 → 그다음에 자르기** 순서인데, 그 순서가 설계에 없습니다. M4에서 `candidate_k`를 활용하거나 `k`에 여유를 두고 인출한 뒤 자르도록 만드시면 됩니다.

## 부록 B — 회귀시키면 안 되는 것들

이미 잘 되어 있어서 건드리면 손해인 것들입니다.

**증거 무결성.** 20개 문서 전부 `source_sha256`과 `source_length`가 코퍼스 파일과 정확히 일치합니다.

**BM25 통계 정합성과 공식.** 불일치 0건이고 공식은 교과서 그대로입니다.

**RRF 구현.** `1/(rrf_k + rank)`, 리스트별 중복 제거, candidate depth 20. 정확합니다.

**벡터 검색.** 청크 자기 텍스트로 검색하면 자기가 유사도 1.0으로 1등에 나옵니다.

**점수 집계 산술.** recall 분모가 전체 정답 span 수이고, MRR이 미적중을 0으로 처리합니다. 흔한 실수를 둘 다 피했습니다.

**`chunk_upsert_statement`의 임베딩 무효화**(`seed.py:670-673`). `index_text`가 바뀔 때만 임베딩을 비웁니다. 비자명하고 올바른 최적화이며, **항목 5 작업 중 가장 깨뜨리기 쉬운 것**이니 특히 주의하세요.

**예외 삼킴이 없다는 것.** `app/`과 `scripts/` 전체에 `except Exception`이 0건입니다. 오류가 조용히 사라지지 않는다는 뜻이고, 이 코드베이스의 큰 장점입니다.

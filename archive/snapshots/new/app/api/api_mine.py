"""Your turn: implement the strict M5 synchronous HTTP API.

Run the reference API tests:
    uv run pytest tests/api

Grade this learner module instead:
    API_MODULE=app.api.api_mine uv run pytest tests/api -v

Missing symbols skip dependent tests while you add strict schemas, injected services,
resource routes, typed 4xx failures, and complete evidence identity.
"""

"""Your turn: implement the complete M4 evidence-checked workflow.

Run the reference workflow tests:
    uv run pytest tests/workflow

Grade this learner package instead:
    WORKFLOW_MODULE=app.workflow.workflow_mine uv run pytest tests/workflow -v

Missing symbols skip their dependent tests while you implement strict state, pure nodes,
typed degradation reasons, the cumulative budget guard, and thin deterministic wiring.
"""

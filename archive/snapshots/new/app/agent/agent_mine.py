"""Your turn: implement the M9 tool-calling agent contract and loop.

Build the strict agent value objects, the tool registry, the providers, the
hand-rolled Thought → Tool → Observation loop, the built-in filing tools, the
query decomposition retriever, and the MCP server described in the M9 build
tutorials.

Grade this learner module instead of the canonical one:

    AGENT_MODULE=app.agent.agent_mine uv run pytest tests/agent -v

This module is intentionally empty until you begin.
"""

"""Your turn: implement M1.4 deterministic PostgreSQL persistence.

Run the reference implementation:
    uv run pytest tests/db

Grade this module instead:
    SEED_MODULE=app.ingestion.seed_mine uv run pytest tests/db -v

Missing seed-layer symbols are skipped, so the test suite remains a progress
board while you implement L1 through L3 from ``docs/en/m1-4-seed/03-build.md``.
This learner module is intentionally empty until you begin L1.
"""

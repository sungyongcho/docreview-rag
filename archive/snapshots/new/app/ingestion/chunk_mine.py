"""Your turn: `ParsedFiling` -> source-cited retrieval chunks.

Run the reference implementation:
    uv run pytest tests/chunk

Grade this module instead:
    CHUNK_MODULE=app.ingestion.chunk_mine uv run pytest tests/chunk -v

Missing symbols are skipped, so pytest remains a progress board while the layers
are implemented. Follow `docs/en/m1-3-chunk/03-build.md` from L1 onward. This file is
intentionally empty until you begin L1.
"""

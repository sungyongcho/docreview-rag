"""Your turn: implement configurable M3 chunking and retrieval ablations.

Run the reference tests:
    uv run pytest tests/evals/test_04_ablation.py

Grade this learner module instead:
    EVAL_ABLATION_MODULE=app.evals.ablation_mine uv run pytest \
        tests/evals/test_04_ablation.py -v

Missing symbols skip dependent tests while you add explicit experiment configs,
stable raw artifact paths, and a deterministic comparison table.
"""

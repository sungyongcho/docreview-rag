"""Your turn: implement async embedding providers and resumable backfill.

Run the reference tests:
    uv run pytest tests/retrieval/test_02_embeddings.py

Grade this learner module instead:
    RETRIEVAL_EMBEDDINGS_MODULE=app.retrieval.embeddings_mine \
        uv run pytest tests/retrieval/test_02_embeddings.py -v

Missing symbols skip their dependent tests, so pytest remains a progress board.
"""

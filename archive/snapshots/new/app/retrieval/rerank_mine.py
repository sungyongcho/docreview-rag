"""Your turn: implement the dependency-free optional reranking boundary.

Run the reference tests:
    uv run pytest tests/retrieval/test_06_rerank.py

Grade this learner module instead:
    RETRIEVAL_RERANK_MODULE=app.retrieval.rerank_mine \
        uv run pytest tests/retrieval/test_06_rerank.py -v

Missing symbols skip their dependent tests, so pytest remains a progress board.
"""
